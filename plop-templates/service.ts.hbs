import { SearchRequest } from "../types/request";

export class {{pascalCase name}}Service {
  public static {{camelCase name}}s = (request: SearchRequest) => {
    const { query } = request;
    const result = [
      {
        name: "Name",
        age: "18",
        address: "Vietnam",
      },
    ];
    return { data: result, metadata: { ...query?.metadata, total: 1 } };
  };
}
