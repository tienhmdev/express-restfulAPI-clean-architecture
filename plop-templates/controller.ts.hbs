import { {{pascalCase name}}Service } from '../services/{{dashCase name}}.service';
import { AppRequest, AppResponse } from '../types/express';
import { httpStatusCodes } from '../utils/constants/httpStatusCodes';
import { responseHelper } from '../utils/helpers';

export class {{pascalCase name}}Controller {
  
  public static {{camelCase name}}s = async (req: AppRequest, res: AppResponse) => {
    try {
      const { data, metadata } = await {{pascalCase name}}Service.{{camelCase name}}s(req);
      responseHelper.sendCollection(res, data, metadata);
    } catch (error) {
      responseHelper.sendError({
        res,
        code: httpStatusCodes.INTERNAL_SERVER_ERROR,
        cause: error,
      });
    }
  };
}
