import express from 'express';
import { {{pascalCase name}}Controller } from '../controllers/{{dashCase name}}.controller';
const {{camelCase name}}Router = express.Router();
import { isEmail } from '../middleware/{{dashCase name}}.middleware';
import { {{camelCase name}}ReqValidator } from '../validations/{{dashCase name}}.validator';

/**
 * GET /api/v1/{{dashCase name}}/{{dashCase name}}s
 * @tags {{pascalCase group}}/{{pascalCase name}}Controller
 * @summary This is the summary of the endpoint
 * @param {LoginRequest} request.body.required - Login User
 * @return {object} 200 - Success Response Token
 * @return {object} 400 - Login Validate Fail Response
 * @return {object} 500 - Internal Error Response
 * @example response - 200 - example success response
 * {
 *   "message": "You have added a song!"
 * }
 * @example response - 400 - example error response
 * {
 *     "message": "Not Found",
 *     "cause": {
 *         "fromDev": "Don't worry, we're fixing this. Please try again after some time."
 *     },
 *     "code": 400
 * }
 */
{{camelCase name}}Router.post('/{{dashCase name}}s', [...{{camelCase name}}ReqValidator, isEmail], {{pascalCase name}}Controller.{{camelCase name}}s);

export default {{dashCase name}}Router;
